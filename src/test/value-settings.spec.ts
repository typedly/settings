import { PatternSettings } from '../interface';

export const patternSettings: PatternSettings<RegExp> = {
  lowercase: true,
  numeric: true,
  pattern: /[a]g/g,
  special: true,
  uppercase: true,
}
